node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Données persistées par le backend (JSON de l'état) — jamais commitées
data/*
!data/.gitkeep

# Override local optionnel (ne doit JAMAIS arriver sur le VPS)
docker-compose.override.yml

# Artefacts d'outils IDE / agent
.claude/
